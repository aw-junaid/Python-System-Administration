# requirements.txt
# ------------------------------------------------------------
# External dependencies for the File & Directory Operations scripts.
#
# Most scripts in this collection use ONLY Python's standard library
# (os, shutil, zipfile, tarfile, hashlib, tempfile, filecmp, difflib, etc.)
# and need NOTHING installed beyond Python itself.
#
# Only two scripts require an external package:
#   - encrypt_files.py
#   - decrypt_files.py
#
# Install with:
#   pip install -r requirements.txt
# ------------------------------------------------------------

cryptography>=42.0.0
